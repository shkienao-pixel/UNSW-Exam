# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Supabase
supabase>=2.5.0

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.2.0
email-validator>=2.1.0

# File upload support
python-multipart>=0.0.9

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0

# LLM / AI (reuses src/services logic via sys.path)
langchain>=0.2.0
langchain-openai>=0.1.0
openai>=1.30.0
tiktoken>=0.7.0

# Document processing
pypdf>=4.2.0
pymupdf>=1.24.0

# Vector store (local, Phase 2 will migrate)
chromadb>=0.5.0

# Google Gemini (text + Imagen 3 image generation)
google-genai>=0.8.0

# Word document parsing (used in fallback extraction)
python-docx>=1.1.0

# JWT signature verification (fallback auth)
PyJWT>=2.8.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
stripe>=8.0.0
